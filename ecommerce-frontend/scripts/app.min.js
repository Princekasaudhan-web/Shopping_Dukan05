const hamburger=document.querySelector(".hamburger"),navbar=document.querySelector(".navbar");hamburger&&navbar&&hamburger.addEventListener("click",()=>{navbar.classList.toggle("active")});const productGrid=document.getElementById("product-grid");function updateCartCount(){let t=JSON.parse(localStorage.getItem("cart"))||[],e=document.querySelector(".cart-count");e&&(e.textContent=t.length)}productGrid&&fetch("https://fakestoreapi.com/products").then(t=>{if(!t.ok)throw Error("Failed to load products.");return t.json()}).then(t=>{t.forEach(t=>{let e=document.createElement("div");e.classList.add("product-card");let a=Math.round(86*t.price);e.innerHTML=`
          <a href="product.html?id=${t.id}" class="product-link">
            <img src="${t.image}" alt="${t.title}" loading="lazy">
            <div class="product-info">
              <h3>${t.title}</h3>
              <p class="price">₹${a}</p>
            </div>
          </a>

          <button class="add-cart"
                  data-id="${t.id}"
                  data-title="${t.title}"
                  data-price="${a}"
                  data-image="${t.image}">
            Add to Cart
          </button>
        `,productGrid.appendChild(e)}),updateCartCount()}).catch(t=>{console.error(t),productGrid.innerHTML="<p>Unable to load products.</p>"}),document.addEventListener("click",function(t){if(t.target.classList.contains("add-cart")){let e={id:t.target.dataset.id,title:t.target.dataset.title,price:t.target.dataset.price,image:t.target.dataset.image},a=JSON.parse(localStorage.getItem("cart"))||[];a.push(e),localStorage.setItem("cart",JSON.stringify(a)),updateCartCount(),alert("Product added to cart!")}}),updateCartCount();const logoutBtn=document.getElementById("logout");logoutBtn&&logoutBtn.addEventListener("click",t=>{t.preventDefault(),localStorage.removeItem("loggedInUser"),window.location.href="login.html"});